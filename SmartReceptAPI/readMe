Post Request for Creating User Account
End Point : http://localhost:8080/api/expenses

Request Body
{
  "name": "Deepanshu",
  "startdate": "2024-14-21",
  "enddate": "2025-8-25",
  "currency": "India",
  "comment": "near trip"
}

Upload an Image: You can use tools like Postman or cURL to test the image upload:

Using cURL:

curl -X POST -F "file=@path_to_image/image.jpg" http://localhost:8080/api/images/upload

Download an Image: You can also use Postman or a browser to download the image:

Using cURL:

curl -O http://localhost:8080/api/images/download/image.jpg

